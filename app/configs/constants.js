const USER_TYPE = {
  CUSTOMER: "customer",
  EMPLOYEE: "employee",
  AFFILIATE: "affiliate"
};

const CATEGORY = {
  GROCERIES: "groceries"
};

module.exports = {
  CATEGORY,
  USER_TYPE
};
